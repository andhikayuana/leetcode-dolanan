package validpolindrom

fun main(): Unit {
    val s = "A man, a plan, a canal: Panama"
}

/**
 * 1 <= s.length <= 2 * 105
 * s consists only of printable ASCII characters.
 */
fun isPalindrome(s: String): Boolean {


}